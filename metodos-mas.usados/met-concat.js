// < ?========= METODO concat =========>

    let array = [1, 2, 3, 4, 5];

    let subArray = array.concat(6,"string");
    console.log(subArray); //[1, 2, 3, 4, 5, 6, 'string'] agrega un numero  adelante y puede agregar string tambien solo adelante.
    
    let string1 = "Hello, world!";
    
    let subString1 = string1.concat(1,"string");
    console.log(subString1); // Hello, world!1string  agrega un numero como string  adelante y puede agregar string tambien solo adelante.